

export enum JsType {
    OBJECT = 'object',
    FUNCTION = 'function',
    STRING = 'string',
    NUMBER = 'number',
    BOOLEAN = 'boolean',
    UNDEFINED = 'undefined',
    SYMBOL = 'symbol',
    BIGINT = 'bigint'
}